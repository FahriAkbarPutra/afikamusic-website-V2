<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login - Afika Music</title>
    <style>
        body {
            margin: 0;
            font-family: 'Montserrat', sans-serif;
            background: radial-gradient(1400px 600px at 50% -20%, rgba(7,193,255,0.35), rgba(4,10,24,1) 55%), #030712;
            min-height: 100vh;
            display: grid;
            place-items: center;
            color: #f8fafc;
        }
        .login-card {
            width: min(360px, 92vw);
            background: rgba(7,12,24,0.85);
            border-radius: 20px;
            padding: 36px 32px;
            border: 1px solid rgba(255,255,255,0.08);
            box-shadow: 0 24px 60px rgba(5,20,45,0.45);
            backdrop-filter: blur(18px);
        }
        h1 { text-align: center; color: #e2e8f0; margin-bottom: 28px; letter-spacing: 0.05em; }
        .form-group { margin-bottom: 18px; display: grid; gap: 8px; }
        label { font-weight: 600; color: #cbd5f5; letter-spacing: 0.02em; }
        input {
            width: 100%;
            padding: 12px 16px;
            border-radius: 12px;
            border: 1px solid rgba(7,193,255,0.35);
            background: rgba(4,8,18,0.85);
            color: white;
            transition: border-color .25s ease, box-shadow .25s ease;
        }
        input:focus {
            outline: none;
            border-color: #07c1ff;
            box-shadow: 0 0 0 3px rgba(7,193,255,0.2);
        }
        button {
            width: 100%;
            padding: 14px;
            border-radius: 999px;
            border: none;
            background: linear-gradient(135deg, #07c1ff, #4adeff);
            color: #041020;
            font-weight: 700;
            letter-spacing: 0.05em;
            cursor: pointer;
            transition: transform .2s ease, box-shadow .25s ease;
        }
        button:hover { transform: translateY(-2px); box-shadow: 0 16px 36px rgba(7,193,255,0.35); }
        .error {
            color: #fca5a5;
            background: rgba(239,68,68,0.12);
            border: 1px solid rgba(239,68,68,0.35);
            padding: 12px 16px;
            border-radius: 12px;
            margin-bottom: 18px;
            text-align: center;
            font-weight: 600;
        }
    </style>
</head>
<body>
    <div class="login-card">
        <h1>Admin Login</h1>
        
        {# Tampilkan pesan error jika ada #}
        {% if error %}
            <div class="error">{{ error }}</div>
        {% endif %}

    <form action="{{ base_path }}/admin/login" method="POST">
            <div class="form-group">
                <label for="username">Username</label>
                <input type="text" id="username" name="username" required>
            </div>
            <div class="form-group">
                <label for="password">Password</label>
                <input type="password" id="password" name="password" required>
            </div>
            <button type="submit">Login</button>
        </form>
    </div>
</body>
</html>