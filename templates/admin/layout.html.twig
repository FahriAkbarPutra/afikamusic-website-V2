<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ page_title|default('Admin Panel') }} - Afika Music</title>
    <style>
        :root {
            --admin-bg: #0d1018;
            --admin-surface: rgba(18, 22, 33, 0.9);
            --admin-border: rgba(255,255,255,0.07);
            --admin-accent: #07c1ff;
            --admin-accent-secondary: #dfff00;
            --admin-text: #f1f5f9;
            --admin-text-muted: #94a3b8;
            --admin-radius-lg: 18px;
            --admin-radius-md: 12px;
            --admin-shadow: 0 18px 42px rgba(3,10,25,0.45);
        }

        * { box-sizing: border-box; }
        body {
            font-family: 'Montserrat', sans-serif;
            margin: 0;
            background: radial-gradient(1200px 600px at 50% -10%, rgba(7,193,255,0.12), rgba(13,16,24,1) 55%), #05070e;
            color: var(--admin-text);
            min-height: 100vh;
        }
        .admin-wrapper { display: grid; grid-template-columns: 280px 1fr; min-height: 100vh; position: relative; }
        .admin-wrapper::before {
            content: "";
            position: fixed;
            inset: 0;
            background: radial-gradient(1600px 600px at 50% 120%, rgba(223,255,0,0.18), rgba(13,16,24,0));
            pointer-events: none;
            z-index: -1;
        }
        .sidebar {
            background: rgba(8,12,20,0.92);
            border-right: 1px solid var(--admin-border);
            backdrop-filter: blur(18px);
            padding: 32px 26px;
            display: flex;
            flex-direction: column;
            gap: 32px;
        }
        .sidebar h2 {
            margin: 0;
            font-size: 1.1rem;
            letter-spacing: 0.08em;
            text-transform: uppercase;
            text-align: center;
            color: var(--admin-text-muted);
        }
        .sidebar nav ul { list-style: none; margin: 0; padding: 0; display: grid; gap: 12px; }
        .sidebar nav a {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 14px 18px;
            text-decoration: none;
            color: var(--admin-text-muted);
            border: 1px solid transparent;
            border-radius: var(--admin-radius-md);
            transition: background .25s ease, border-color .25s ease, color .25s ease, transform .25s ease;
            background: rgba(255,255,255,0.02);
        }
        .sidebar nav a:hover,
        .sidebar nav a.is-active {
            color: var(--admin-text);
            border-color: rgba(7,193,255,0.35);
            background: linear-gradient(120deg, rgba(7,193,255,0.16), rgba(223,255,0,0.08));
            transform: translateY(-2px);
            box-shadow: 0 12px 24px rgba(7,193,255,0.2);
        }
        .main-content {
            padding: 40px clamp(32px, 4vw, 64px);
            background: linear-gradient(180deg, rgba(13,16,24,0.9), rgba(7,10,20,0.95));
        }
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 28px;
        }
        .header h1 {
            margin: 0;
            font-size: clamp(1.6rem, 2vw, 2rem);
            letter-spacing: 0.02em;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            background: rgba(11,15,24,0.85);
            border: 1px solid var(--admin-border);
            border-radius: var(--admin-radius-md);
            overflow: hidden;
            box-shadow: var(--admin-shadow);
        }
        th, td {
            padding: 16px 20px;
            border-bottom: 1px solid rgba(255,255,255,0.04);
            color: var(--admin-text-muted);
        }
        th { text-transform: uppercase; letter-spacing: 0.08em; font-size: 0.75rem; color: var(--admin-text); background: rgba(7,11,19,0.95); }
        tbody tr:hover { background: rgba(7,193,255,0.05); }
        .btn {
            padding: 10px 18px;
            border-radius: 999px;
            text-decoration: none;
            border: none;
            cursor: pointer;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 6px;
            font-weight: 600;
            transition: transform .2s ease, box-shadow .25s ease, filter .2s ease;
        }
        .btn-primary { background: linear-gradient(135deg, #07c1ff, #68e0ff); color: #030712; box-shadow: 0 12px 24px rgba(7,193,255,0.35); }
        .btn-warning { background: linear-gradient(135deg, #facc15, #fde047); color: #111827; box-shadow: 0 12px 24px rgba(250,204,21,0.25); }
        .btn-danger { background: linear-gradient(135deg, #f97316, #ef4444); color: #111827; box-shadow: 0 12px 24px rgba(239,68,68,0.25); }
        .btn:hover { transform: translateY(-2px); filter: brightness(1.05); }
        .form-card {
            background: rgba(11,15,24,0.85);
            border-radius: var(--admin-radius-lg);
            border: 1px solid var(--admin-border);
            padding: clamp(28px, 4vw, 40px);
            box-shadow: var(--admin-shadow);
        }
        .form-group { margin-bottom: 18px; }
        .form-group label { display: block; margin-bottom: 6px; font-weight: 600; color: var(--admin-text); letter-spacing: 0.02em; }
        .form-group input,
        .form-group textarea,
        .form-group select {
            width: 100%;
            padding: 12px 16px;
            border-radius: var(--admin-radius-md);
            border: 1px solid rgba(7,193,255,0.25);
            background: rgba(7,12,20,0.85);
            color: var(--admin-text);
            transition: border-color .25s ease, box-shadow .25s ease;
        }
        .form-group input:focus,
        .form-group textarea:focus,
        .form-group select:focus {
            outline: none;
            border-color: var(--admin-accent);
            box-shadow: 0 0 0 3px rgba(7,193,255,0.2);
        }
        .status-badge {
            display: inline-flex;
            align-items: center;
            gap: 6px;
            padding: 6px 14px;
            border-radius: 999px;
            font-size: 0.85rem;
            font-weight: 600;
        }
        .status-active { background: rgba(34,197,94,0.15); color: #4ade80; }
        .status-inactive { background: rgba(248,113,113,0.12); color: #fca5a5; }
        .table-actions { display: flex; gap: 10px; flex-wrap: wrap; }
        .top-toolbar { display: flex; justify-content: space-between; align-items: center; margin-bottom: 24px; flex-wrap: wrap; gap: 16px; }
        .top-toolbar a { text-decoration: none; }
        .toolbar-info { color: var(--admin-text-muted); font-size: 0.9rem; }

        @media (max-width: 960px) {
            .admin-wrapper { grid-template-columns: 1fr; }
            .sidebar { position: sticky; top: 0; z-index: 10; flex-direction: row; overflow-x: auto; backdrop-filter: blur(22px); border-right: none; border-bottom: 1px solid var(--admin-border); }
            .sidebar nav ul { grid-auto-flow: column; grid-auto-columns: max-content; }
            .sidebar nav a { border-radius: 999px; }
        }
        @media (max-width: 600px) {
            .main-content { padding: 32px 20px; }
            table, thead { display: none; }
            tbody { display: grid; gap: 18px; }
            tbody tr { display: grid; gap: 12px; border: 1px solid var(--admin-border); border-radius: var(--admin-radius-md); padding: 18px; background: rgba(11,15,24,0.9); }
            tbody tr td { border: none; padding: 0; }
            .table-actions { justify-content: flex-start; }
        }
    </style>
</head>
<body>
    <div class="admin-wrapper">
        <aside class="sidebar">
            <h2>Afika Music</h2>
            <nav>
                <ul>
                    <li><a href="{{ base_path }}/admin/panel" class="{{ current_path starts with base_path ~ '/admin/panel' ? 'is-active' }}">Dashboard</a></li>
                    <li><a href="{{ base_path }}/admin/services" class="{{ current_path starts with base_path ~ '/admin/services' ? 'is-active' }}">Kelola Layanan</a></li>
                    <li><a href="{{ base_path }}/admin/bookings" class="{{ current_path starts with base_path ~ '/admin/bookings' ? 'is-active' }}">Kelola Pesanan</a></li>
                    <li><a href="{{ base_path }}/admin/schedule" class="{{ current_path starts with base_path ~ '/admin/schedule' ? 'is-active' }}">Kelola Jadwal</a></li>
                    <li><a href="{{ base_path }}/admin/settings" class="{{ current_path starts with base_path ~ '/admin/settings' ? 'is-active' }}">Pengaturan</a></li>
                    <li><a href="{{ base_path }}/admin/logout">Logout</a></li>
                </ul>
            </nav>
        </aside>
        <main class="main-content">
            <div class="header">
                <h1>{{ page_title|default('Dashboard') }}</h1>
            </div>
            
            {# Notifikasi flash (untuk pesan sukses/error) #}
            {% if flash %}
            <div class="flash-{{ flash.type }}" style="padding: 15px; margin-bottom: 20px; background-color: {{ flash.type == 'success' ? '#d4edda' : '#f8d7da' }}; color: {{ flash.type == 'success' ? '#155724' : '#721c24' }}; border: 1px solid {{ flash.type == 'success' ? '#c3e6cb' : '#f5c6cb' }}; border-radius: 4px;">
                {{ flash.message }}
            </div>
            {% endif %}

            {% block content %}{% endblock %}
        </main>
    </div>
</body>
</html>