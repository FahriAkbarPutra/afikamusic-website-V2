<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ page_title|default('Admin Panel') }} - Afika Music</title>
    <style>
        /* CSS Sederhana untuk Admin Panel */
        body { font-family: sans-serif; margin: 0; background-color: #f4f7f6; }
        .admin-wrapper { display: flex; }
        .sidebar { width: 220px; background: #2c3e50; color: white; min-height: 100vh; padding-top: 20px; }
        .sidebar h2 { text-align: center; }
        .sidebar nav ul { list-style: none; padding: 0; margin: 0; }
        .sidebar nav a { display: block; padding: 15px 20px; color: white; text-decoration: none; border-bottom: 1px solid #34495e; }
        .sidebar nav a:hover { background-color: #34495e; }
        .main-content { flex-grow: 1; padding: 30px; }
        .header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px; }
        table { width: 100%; border-collapse: collapse; background: white; }
        th, td { padding: 12px; border: 1px solid #ddd; text-align: left; }
        th { background-color: #ecf0f1; }
        .btn { padding: 8px 15px; border-radius: 4px; text-decoration: none; border: none; cursor: pointer; display: inline-block; }
        .btn-primary { background-color: #3498db; color: white; }
        .btn-warning { background-color: #f1c40f; color: #333; }
        .btn-danger { background-color: #e74c3c; color: white; }
        .form-group { margin-bottom: 15px; }
        .form-group label { display: block; margin-bottom: 5px; }
        .form-group input, .form-group textarea, .form-group select { width: 100%; padding: 10px; border: 1px solid #ccc; border-radius: 4px; box-sizing: border-box; }
    </style>
</head>
<body>
    <div class="admin-wrapper">
        <aside class="sidebar">
            <h2>Afika Music</h2>
            <nav>
                <ul>
                    <li><a href="/afika-music/public/admin/panel">Dashboard</a></li>
                    <li><a href="/admin/services">Kelola Layanan</a></li>
                    <li><a href="/admin/bookings">Kelola Pesanan</a></li>
                    <li><a href="/admin/settings">Pengaturan</a></li>
                    <li><a href="/admin/logout">Logout</a></li>
                </ul>
            </nav>
        </aside>
        <main class="main-content">
            <div class="header">
                <h1>{{ page_title|default('Dashboard') }}</h1>
            </div>
            
            {# Notifikasi flash (untuk pesan sukses/error) #}
            {% if flash %}
            <div class="flash-{{ flash.type }}" style="padding: 15px; margin-bottom: 20px; background-color: {{ flash.type == 'success' ? '#d4edda' : '#f8d7da' }}; color: {{ flash.type == 'success' ? '#155724' : '#721c24' }}; border: 1px solid {{ flash.type == 'success' ? '#c3e6cb' : '#f5c6cb' }}; border-radius: 4px;">
                {{ flash.message }}
            </div>
            {% endif %}

            {% block content %}{% endblock %}
        </main>
    </div>
</body>
</html>