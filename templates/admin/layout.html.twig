<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ page_title|default('Admin Panel') }} - Afika Music</title>
    <link rel="stylesheet" href="{{ base_path }}/assets/css/vendor/leaflet.css">
    <style>
        :root {
            --admin-bg: #f5f7fb;
            --admin-surface: #ffffff;
            --admin-nav-bg: #111827;
            --admin-nav-active: #0ea5e9;
            --admin-border: #e5e7eb;
            --admin-text: #1f2937;
            --admin-text-muted: #6b7280;
            --admin-radius-md: 10px;
            --admin-radius-lg: 16px;
            --admin-shadow: 0 12px 28px rgba(15,23,42,0.12);
        }

        * { box-sizing: border-box; }
        body {
            font-family: 'Montserrat', sans-serif;
            margin: 0;
            background: var(--admin-bg);
            color: var(--admin-text);
        }
        .admin-wrapper { display: flex; min-height: 100vh; }
        .sidebar {
            width: 250px;
            background: var(--admin-nav-bg);
            color: #f9fafb;
            display: flex;
            flex-direction: column;
            padding: 32px 20px;
            gap: 32px;
        }
        .sidebar h2 {
            margin: 0;
            font-size: 1.1rem;
            text-align: center;
            letter-spacing: 0.08em;
            text-transform: uppercase;
            color: rgba(241,245,249,0.7);
        }
        .sidebar nav ul { list-style: none; margin: 0; padding: 0; display: grid; gap: 12px; }
        .sidebar nav a {
            display: block;
            padding: 12px 16px;
            border-radius: var(--admin-radius-md);
            color: rgba(241,245,249,0.85);
            text-decoration: none;
            transition: background .2s ease, color .2s ease;
        }
        .sidebar nav a:hover,
        .sidebar nav a.is-active {
            background: rgba(14,165,233,0.15);
            color: #e0f2fe;
        }
        .main-content {
            flex: 1;
            padding: 32px clamp(20px, 4vw, 56px);
            display: flex;
            flex-direction: column;
            gap: 24px;
        }
        .header {
            display: flex;
            align-items: center;
            justify-content: space-between;
        }
        .header h1 {
            margin: 0;
            font-size: clamp(1.4rem, 2.5vw, 1.9rem);
        }
        .content-card {
            background: var(--admin-surface);
            border: 1px solid var(--admin-border);
            border-radius: var(--admin-radius-lg);
            padding: clamp(20px, 3vw, 28px);
            box-shadow: var(--admin-shadow);
        }
        table {
            width: 100%;
            border-collapse: collapse;
        }
        th, td {
            padding: 14px 16px;
            border-bottom: 1px solid var(--admin-border);
            font-size: 0.95rem;
        }
        th {
            text-align: left;
            font-size: 0.8rem;
            text-transform: uppercase;
            color: var(--admin-text-muted);
            letter-spacing: 0.08em;
        }
        tbody tr:last-child td { border-bottom: none; }
        tbody tr:hover { background: #f0f9ff; }
        .btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            padding: 10px 16px;
            border-radius: 999px;
            border: none;
            font-weight: 600;
            cursor: pointer;
            text-decoration: none;
            transition: background .2s ease, transform .2s ease;
        }
        .btn-primary { background: #0ea5e9; color: #f8fafc; }
        .btn-warning { background: #facc15; color: #1f2937; }
        .btn-danger { background: #ef4444; color: #f8fafc; }
        .btn:hover { transform: translateY(-1px); }
        .btn:focus-visible { outline: 2px solid #22d3ee; outline-offset: 2px; }
        .form-card { background: var(--admin-surface); border: 1px solid var(--admin-border); border-radius: var(--admin-radius-lg); padding: clamp(20px, 3vw, 32px); box-shadow: var(--admin-shadow); }
        .form-group { margin-bottom: 16px; }
        .form-group label { display: block; margin-bottom: 6px; font-weight: 600; color: var(--admin-text); }
        .form-group input,
        .form-group textarea,
        .form-group select {
            width: 100%;
            padding: 10px 14px;
            border-radius: var(--admin-radius-md);
            border: 1px solid var(--admin-border);
            font-size: 0.95rem;
            transition: border-color .2s ease, box-shadow .2s ease;
        }
        .form-group textarea { resize: vertical; }
        .form-group input:focus,
        .form-group textarea:focus,
        .form-group select:focus {
            outline: none;
            border-color: #0ea5e9;
            box-shadow: 0 0 0 3px rgba(14,165,233,0.2);
        }
        .status-badge {
            display: inline-flex;
            align-items: center;
            padding: 6px 12px;
            border-radius: 999px;
            font-size: 0.78rem;
            font-weight: 600;
        }
        .status-active { background: #dcfce7; color: #166534; }
        .status-inactive { background: #fee2e2; color: #b91c1c; }
        .table-actions { display: flex; gap: 8px; flex-wrap: wrap; }
        .top-toolbar { display: flex; justify-content: space-between; align-items: center; gap: 16px; flex-wrap: wrap; }
        .toolbar-info { color: var(--admin-text-muted); font-size: 0.9rem; }
        
        /* Leaflet Map Styles */
        .leaflet-map {
            height: 300px;
            border-radius: var(--admin-radius-md);
            border: 1px solid var(--admin-border);
            z-index: 1;
            position: relative;
            background: #f8f9fa;
        }
        .leaflet-container {
            border-radius: var(--admin-radius-md);
        }
        .leaflet-popup-content {
            margin: 8px 12px;
            line-height: 1.4;
        }
        .location-search {
            display: flex;
            gap: 8px;
            align-items: center;
        }
        .location-search input {
            flex: 1;
        }
        .location-search__btn {
            padding: 10px 16px;
            white-space: nowrap;
            flex-shrink: 0;
        }

        @media (max-width: 960px) {
            .admin-wrapper { flex-direction: column; }
            .sidebar {
                width: 100%;
                flex-direction: row;
                align-items: center;
                justify-content: space-between;
                gap: 16px;
                position: sticky;
                top: 0;
                z-index: 20;
            }
            .sidebar nav ul { display: flex; gap: 10px; overflow-x: auto; }
            .sidebar nav a { white-space: nowrap; }
        }
        @media (max-width: 720px) {
            .main-content { padding: 20px; }
            table, thead { display: none; }
            tbody { display: grid; gap: 16px; }
            tbody tr { display: grid; gap: 10px; padding: 16px; border: 1px solid var(--admin-border); border-radius: var(--admin-radius-md); background: var(--admin-surface); }
            tbody tr td { padding: 0; border: none; }
        }
    </style>
</head>
<body data-base-path="{{ base_path }}">
    <div class="admin-wrapper">
        <aside class="sidebar">
            <h2>Afika Music</h2>
            <nav>
                <ul>
                    <li><a href="{{ base_path }}/admin/panel" class="{{ current_path starts with base_path ~ '/admin/panel' ? 'is-active' }}">Dashboard</a></li>
                    <li><a href="{{ base_path }}/admin/services" class="{{ current_path starts with base_path ~ '/admin/services' ? 'is-active' }}">Kelola Layanan</a></li>
                    <li><a href="{{ base_path }}/admin/bookings" class="{{ current_path starts with base_path ~ '/admin/bookings' ? 'is-active' }}">Kelola Pesanan</a></li>
                    <li><a href="{{ base_path }}/admin/schedule" class="{{ current_path starts with base_path ~ '/admin/schedule' ? 'is-active' }}">Kelola Jadwal</a></li>
                    <li><a href="{{ base_path }}/admin/settings" class="{{ current_path starts with base_path ~ '/admin/settings' ? 'is-active' }}">Pengaturan</a></li>
                    <li><a href="{{ base_path }}/admin/logout">Logout</a></li>
                </ul>
            </nav>
        </aside>
        <main class="main-content">
            <div class="header">
                <h1>{{ page_title|default('Dashboard') }}</h1>
            </div>

            {% if flash %}
                <div class="content-card" style="border-left: 4px solid {{ flash.type == 'success' ? '#22c55e' : '#ef4444' }};">
                    <strong style="display:block; margin-bottom:6px;">{{ flash.type == 'success' ? 'Berhasil' : 'Terjadi Kesalahan' }}</strong>
                    <span style="color: var(--admin-text-muted);">{{ flash.message }}</span>
                </div>
            {% endif %}

            {% block content %}{% endblock %}
        </main>
    </div>
</body>
<script src="{{ base_path }}/assets/js/vendor/leaflet.js"></script>
<script src="{{ base_path }}/assets/js/main.js"></script>
</html>